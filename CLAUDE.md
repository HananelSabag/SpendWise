# CLAUDE.md — SpendWise (auto-loaded project memory)

**Canonical context primer — read it fully before doing anything:** @AGENTS.md

`AGENTS.md` is the single source of truth (stack, run commands, domain model, full Supabase DB map,
server+client code maps, conventions, open work). It is shared verbatim with Codex / Cursor / other
models. **Edit `AGENTS.md`, not this file** — this file only imports it so Claude auto-loads it.

Sibling repo (the scraper worker): `C:\CodingProjects\spendwise-agent` — see its own `AGENTS.md`.
Deeper backstory (Claude private memory): `C:\Users\hanan\.claude\projects\C--CodingProjects\memory\project_spendwise.md`.
